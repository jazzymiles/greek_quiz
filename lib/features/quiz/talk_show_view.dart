import 'package:flutter/material.dart';
class TalkShowView extends StatelessWidget {
  const TalkShowView({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Card Mode"));
}